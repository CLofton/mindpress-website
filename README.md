# MindPress static site

GitHub Pages files for **mindpress.ai**. Custom domain stays `mindpress.ai` via `CNAME`. Do not change DNS.

## Offer lock

One desk: we follow up until they book. You close. Agents run the desk overnight.

- H1: We follow up until they book.
- CTA: send last week’s inquiry or partner log to `hello.mindpress@agentmail.to` (totals only).
- 5-day leak map, then `$4,000/month` desk, 90-day minimum.
- Three use cases: career-school enrollment, host-practice 1-BD follow-up, multi-unit AUV / return visits.
- Kill: diagnostic as front door, four-bucket FDE, ads/SEO/social, book-a-call, fictional logos.

Forms on `book.html` and `intake.html` post via FormSubmit AJAX to `hello.mindpress@agentmail.to`. Never restore `hello@mindpress.ai` (no MX). No Formspree `YOUR_ID`. No Calendly.

Pages source: `main` root. Push via Git Data API as CLofton. No force push. No clone required.
